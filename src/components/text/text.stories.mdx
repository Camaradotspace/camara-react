import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  PRIMARY_STORY,
} from '@storybook/addon-docs/blocks';
import { Text } from './text';

<Meta title='Components/Text' component={Text} />

export const argTypes = {
  color: { control: 'color' },
};

# Text

Texts are used to indicate whatever

## Usage

Text should be Text

<Canvas withToolbar>
  <Story name='Title' args={{}} argTypes={argTypes}>
    {(args) => (
      <Text variant='title' {...args}>
        Building systems and tools for the people who build things
      </Text>
    )}
  </Story>
  <Story name='Heading' args={{}} argTypes={argTypes}>
    {(args) => (
      <Text variant='heading' {...args}>
        Building systems and tools for the people who build things
      </Text>
    )}
  </Story>
  <Story name='Sub-Heading' args={{}} argTypes={argTypes}>
    {(args) => (
      <Text variant='sub-heading' {...args}>
        Building systems and tools for the people who build things
      </Text>
    )}
  </Story>
  <Story name='Body' args={{}} argTypes={argTypes}>
    {(args) => (
      <Text variant='body' {...args}>
        Building systems and tools for the people who build things
      </Text>
    )}
  </Story>
  <Story name='Caption' args={{}} argTypes={argTypes}>
    {(args) => (
      <Text variant='caption' {...args}>
        Building systems and tools for the people who build things
      </Text>
    )}
  </Story>
  <Story name='Overline' args={{}} argTypes={argTypes}>
    {(args) => (
      <Text variant='overline' {...args}>
        Building systems and tools for the people who build things
      </Text>
    )}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY} />

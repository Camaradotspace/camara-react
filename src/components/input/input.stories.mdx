import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  PRIMARY_STORY,
} from '@storybook/addon-docs/blocks';
import { Input } from './input';

<Meta title='Components/Input' component={Input} />

export const argTypes = {
  backgroundColor: { control: 'color' },
  color: { control: 'color' },
  placeholderColor: { control: 'color' },
  borderColor: { control: 'color' },
};

# Input

Inputs are used to indicate whatever

## Usage

Input should be Input

<Canvas withToolbar>
  <Story name='Input' args={{}} argTypes={argTypes}>
    {(args) => <Input {...args} />}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY} />

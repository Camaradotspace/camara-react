import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  PRIMARY_STORY,
} from '@storybook/addon-docs/blocks';
import { TextArea } from './textarea';

<Meta title='Components/TextArea' component={TextArea} />

export const argTypes = {
  color: { control: 'color' },
};

# TextArea

TextAreas are used to indicate whatever

## Usage

TextArea should be TextArea

<Canvas withToolbar>
  <Story name='TextArea' args={{}} argTypes={argTypes}>
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY} />

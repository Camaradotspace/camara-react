import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  PRIMARY_STORY,
} from '@storybook/addon-docs/blocks';
import { Link } from './link';

<Meta title='Components/Link' component={Link} />

export const argTypes = {
  backgroundColor: { control: 'color' },
  color: { control: 'color' },
};

# Link

Links are used to indicate whatever

## Usage

Link should be Link

<Canvas withToolbar>
  <Story name='Link' args={{}} argTypes={argTypes}>
    {(args) => <Link {...args}>Click me please</Link>}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY} />

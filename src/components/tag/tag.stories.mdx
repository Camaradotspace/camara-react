import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  PRIMARY_STORY,
} from '@storybook/addon-docs/blocks';
import { Tag } from './tag';

<Meta title='Components/Tag' component={Tag} />

export const argTypes = {
  color: { control: 'color' },
};

# Tag

Tags are used to indicate whatever

## Usage

Tag should be Tag

<Canvas withToolbar>
  <Story name='Resolved' args={{}} argTypes={argTypes}>
    {(args) => (
      <Tag color='#bb65ff' {...args}>
        resolved
      </Tag>
    )}
  </Story>
  <Story name='Open' args={{}} argTypes={argTypes}>
    {(args) => (
      <Tag color='#ee3b5b' {...args}>
        open
      </Tag>
    )}
  </Story>
  <Story name='In-Review' args={{}} argTypes={argTypes}>
    {(args) => (
      <Tag color='#0099ff' {...args}>
        in-review
      </Tag>
    )}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY} />
